"use strict" // treat all JS code as newer version 

// alert(4+4);  this will not work in Node, as there is a different syntax to do so. It works in browser because the code runs in the form of a doc


let name = "Sankalp" //string DT
let age = 20 // number
let isLoggedIn = true // boolean

//primitive data types:
//null -> standalone value // khali value
//undefined -> value is not defined
// symbol -> to find uniquness 


//object

console.log(typeof null); // it will show "object" as a type
console.log(typeof undefined); // it will show "undefined" as a type
